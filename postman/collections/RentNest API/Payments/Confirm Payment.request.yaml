$kind: http-request
url: "{{baseUrl}}/api/payments/confirm"
method: POST
headers:
  Content-Type: application/x-www-form-urlencoded
body:
  type: urlencoded
  content:
    tran_id: "{{transactionId}}"
    val_id: "{{validationId}}"
    status: VALID
order: 4000
