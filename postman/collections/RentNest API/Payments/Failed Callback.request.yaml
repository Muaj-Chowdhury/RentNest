$kind: http-request
url: "{{baseUrl}}/api/payments/fail"
method: POST
headers:
  Content-Type: application/x-www-form-urlencoded
body:
  type: urlencoded
  content:
    status: FAILED
    tran_id: "{{transactionId}}"
    verify_sign: "{{verifySign}}"
    verify_key: "{{verifyKey}}"
order: 7000
